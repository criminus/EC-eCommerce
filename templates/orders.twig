<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{sitename}} - {{pageTitle}}</title>
    {% include 'header.twig' %}
</head>
<body>

      {% include 'navigation.twig' %}

      <header class="bg-dark text-white text-center py-5" style="background-image: url('assets/css/images/header.jpg'); background-size: cover; background-position: center;">
        <div class="container">
          <h1 class="display-4">MKTime</h1>
          <p class="lead">Luxury header with a luxury description for a nice website.</p>
          <a href="{{pages.products.url}}" class="btn btn-outline-light btn-lg">Shop now</a>
        </div>
      </header>

      <section class="secret text-center">
        <h1 class="display-4"><iconify-icon inline="true" icon="dashicons:cart"  style="color: black"></iconify-icon> Previous orders</h1>
        <hr>
        <div class="secret-block">
            {% if previousOrders is not empty %}
            <div class="table-responsive-lg">
                <table class="table">
                <thead>
                    <tr>
                    <th scope="col">#</th>
                    <th scope="col">Order Date</th>
                    <th scope="col">Total Amount</th>
                    </tr>
                </thead>
                <tbody>
                {% for order in previousOrders %}
                    <tr>
                    <th scope="row">{{order.order_id}}</th>
                    <td>{{order.order_date}}</td>
                    <td>{{order.total_amount}}</td>
                    </tr>
                {% endfor %}
                </tbody>
                </table>
            </div>
            {% else %}
                <p>You don't have any previous orders.</p>
            {% endif %}
        </div>
      </section>

      {% include 'footer.twig' %}